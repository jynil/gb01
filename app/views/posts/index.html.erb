<% @posts.each do |post| %>
<div>
  <div>
    <!-- 顯示 post.title 和 content -->
    <h3><%= post.title %></h3>
    <p><%= post.content %></p>
    <span>
      <!-- link_to 會產生出 html <a> tag -->
      <%= link_to "show post", post_path(post)%>
    </span>
  </div>
</div>
<% end %>